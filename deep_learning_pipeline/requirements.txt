# Core SfM stack: SuperPoint + LightGlue for correspondences,
# hloc to glue them to a COLMAP database, pycolmap for incremental
# mapping + bundle adjustment.
torch>=2.0
torchvision>=0.15
numpy<2.0
opencv-python
h5py
tqdm
pycolmap>=0.6.1

# Optional, only needed for pipeline/visualize.py
open3d

# ---------------------------------------------------------------------------
# hloc install note (see deep_learning_pipeline/README.md, section 1):
#   cd Hierarchical-Localization && pip install -e .
# installs hloc + its requirements (including lightglue from cvg/LightGlue
# and pycolmap>=3.13.0). This file intentionally keeps the repo's base deps
# so the classical pipeline stays lightweight; the deep pipeline gets the
# rest from the editable hloc install.
# ---------------------------------------------------------------------------
